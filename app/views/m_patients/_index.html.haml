.buttons
  = link_to "#{icon(:plus, :white)} Add Martini Patient".html_safe,
            '#',
            class: 'btn btn-info add_m_patient'
.fat-rows
  - m_patients.each do |m_patient|
    .fat-row
      = render partial: 'm_patients/show', locals: {m_patient: m_patient}

:coffeescript
  $ ->
    $('.add_m_patient').click ->
      # open edit view here
      false

    $('.edit_m_patient').click ->
      # open edit view here
      false
